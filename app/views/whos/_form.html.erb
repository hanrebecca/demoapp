<%= form_for(@who) do |f| %>
  <% if @who.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@who.errors.count, "error") %> prohibited this who from being saved:</h2>

      <ul>
      <% @who.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :are %><br>
    <%= f.text_field :are %>
  </div>
  <div class="field">
    <%= f.label :you %><br>
    <%= f.text_field :you %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
